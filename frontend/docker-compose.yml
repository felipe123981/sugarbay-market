services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: sugarbay-webapp
    extra_hosts:
      - "host.docker.internal:host-gateway"
